{
  "name": "cafe-indica",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack app to browse, filter, search and review coffee products.",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "setup": "npm install && cd backend && npm install",
    "seed": "node backend/seed.js cleaned_coffee_products.json",
    "dev": "concurrently \"npm:dev:backend\" \"npm:dev:fastapi\" \"npm:dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:fastapi": "cd backend-py && uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "npm --workspace frontend run dev",
    "build": "cd backend && npm run build && npm --workspace frontend run build",
    "start": "cd backend && npm run start",
    "test": "cd backend && npm test && npm --workspace frontend test"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "csv-parse": "^6.1.0"
  }
}
